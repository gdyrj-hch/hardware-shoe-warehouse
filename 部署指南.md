# 硬货鞋仓官网部署指南

## 当前状态
✅ 本地 Git 仓库已初始化（位于 `temp/第2周/官网/`）
✅ 所有官网文件已提交（6个HTML页面 + 静态资源）
❌ GitHub 远程仓库尚未创建（需要手动创建）

## 第一步：创建 GitHub 仓库

### 操作步骤
1. 访问 [GitHub 官网](https://github.com) 并登录你的账户
2. 点击右上角 **+** 图标，选择 **New repository**
3. 填写仓库信息：
   - **Repository name**: `hardware-shoe-warehouse`（建议使用此名称，或自定义）
   - **Description**: `硬货鞋仓官方网站 - 硬折扣鞋类加盟品牌`
   - **Visibility**: **Public**（必须公开才能使用免费托管）
   - **Initialize this repository with**: **不要勾选**（因为我们已有本地代码）
4. 点击 **Create repository**

### 创建后获取远程URL
创建成功后，你将看到类似这样的远程URL：
- **HTTPS**: `https://github.com/你的用户名/hardware-shoe-warehouse.git`
- **SSH**: `git@github.com:你的用户名/hardware-shoe-warehouse.git`

## 第二步：关联远程仓库并推送代码

在本地终端中执行以下命令（确保当前目录为 `temp/第2周/官网/`）：

```bash
# 添加远程仓库（替换为你的实际URL）
git remote add origin https://github.com/你的用户名/hardware-shoe-warehouse.git

# 将本地分支重命名为 main（可选，GitHub默认分支名）
git branch -M main

# 推送代码到远程仓库
git push -u origin main
```

如果遇到认证问题，可能需要：
1. 使用GitHub Personal Access Token（创建方式见附录）
2. 或使用SSH密钥（配置方式见附录）

## 第三步：选择免费托管方案

### 方案一：GitHub Pages（最简单）
**前提**：代码已推送到GitHub仓库的 `main` 分支

**部署步骤**：
1. 进入仓库页面，点击 **Settings** → **Pages**
2. 在 **Source** 下拉菜单中选择 **Deploy from a branch**
3. 在 **Branch** 中选择 `main`，文件夹选择 `/ (root)`
4. 点击 **Save**，等待1-2分钟

**访问地址**：
- `https://你的用户名.github.io/hardware-shoe-warehouse/`

**注意事项**：
- 确保 `index.html` 在根目录下
- 如果页面样式错乱，检查CSS/JS路径是否为相对路径

### 方案二：Vercel（专业、快速）
**优势**：自动SSL、全球CDN、支持自定义域名

**部署步骤**：
1. 访问 [Vercel官网](https://vercel.com) 并登录（支持GitHub账号登录）
2. 点击 **Add New...** → **Project**
3. 导入 `hardware-shoe-warehouse` 仓库
4. 保持默认设置，点击 **Deploy**

**访问地址**：
- 自动生成：`https://hardware-shoe-warehouse.vercel.app`
- 可绑定自定义域名

### 方案三：Netlify（功能丰富）
**优势**：免费HTTPS、表格处理、A/B测试

**部署步骤**：
1. 访问 [Netlify官网](https://netlify.com) 并登录（支持GitHub账号登录）
2. 点击 **Add new site** → **Import an existing project**
3. 选择GitHub，授权后选择 `hardware-shoe-warehouse` 仓库
4. 保持默认设置，点击 **Deploy site**

**访问地址**：
- 自动生成：`https://random-name.netlify.app`
- 可自定义域名

## 第四步：验证部署

部署成功后，请访问生成的URL，检查：
- [ ] 所有页面能正常访问
- [ ] 导航菜单功能正常
- [ ] 图片和样式加载正确
- [ ] 表单功能正常（如有）

## 附录

### A. 创建GitHub Personal Access Token
1. 进入GitHub → **Settings** → **Developer settings** → **Personal access tokens** → **Tokens (classic)**
2. 点击 **Generate new token (classic)**
3. 填写 **Note**（如 "硬货鞋仓部署"）
4. 选择 **repo** 权限
5. 点击 **Generate token**，复制生成的token（只会显示一次）

**使用方式**：
- 在命令行输入密码时，**使用token代替密码**
- 或设置环境变量

### B. 配置SSH密钥
1. 生成SSH密钥：`ssh-keygen -t ed25519 -C "your_email@example.com"`
2. 将公钥添加到GitHub：**Settings** → **SSH and GPG keys** → **New SSH key**
3. 粘贴 `~/.ssh/id_ed25519.pub` 内容

### C. 本地开发预览
如需在本地预览，可在 `temp/第2周/官网/` 目录下运行：
```bash
python3 -m http.server 8080
```
然后在浏览器访问 `http://localhost:8080`

---

## 后续衔接
官网部署完成后，请将正式URL更新至：
- 社交媒体账号资料（抖音、知乎、小红书）
- 后续媒体报道素材中
- 第5周（3月30日）的媒体合作策划将基于此正式入口展开

---

> **部署时间建议**：请在本周内完成部署，确保第5周媒体合作策划能有效衔接。
> **技术支持**：如遇部署问题，可提供错误信息，我将协助排查。